#use of default parameters
def tasks(name, task='Assignment', branch='BCA'):
    print(f"{name} has completed the {task} of {branch}")
#Passing two arguments only
tasks('VARNIKA', 'Python')
#Passing one argument only
tasks('VARNIKA')
